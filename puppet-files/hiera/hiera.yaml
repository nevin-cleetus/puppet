---
version: 5
hierarchy:

  - name: "Per-node data"
    path: "nodes/%{::trusted.certname}.yaml"
  - name: "Per OS Family"
    path: "os/%{facts.os.family}.yaml"
  - name: "Other YAML hierarchy levels"
    paths:
      - "common.yaml"
~

